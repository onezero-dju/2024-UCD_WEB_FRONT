import React from 'react'
import MainContainer from '../../components/MainContainer/MainContainer';

function MainPage({selectedChannel}) {
  return (
    <MainContainer selectedChannel={selectedChannel}/>
  )
}

export default MainPage
